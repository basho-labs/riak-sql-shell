require 'spec_helper'

describe Riak::Sql::Shell do
  it 'has a version number' do
    expect(Riak::Sql::Shell::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
